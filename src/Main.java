import graphs.Graph;
import heaps.Heap;
import heaps.MaxHeap;
import tries.Trie;

import java.util.*;

public class Main {
    public static void main(String[] args) {
        Graph graph = new Graph();

        graph.addNode("Booty");
        graph.addNode("Cheeks");

        graph.print();
    }
}